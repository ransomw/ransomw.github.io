define([
], function () {
    "use strict";

    var server_domain = 'localhost',
        server_port = 3000;

    return {
        server_domain: server_domain,
        server_port: server_port
    };

});