define([
], function () {
    "use strict";

    var server_domain = '192.241.218.140',
        server_port = 3000;

    return {
        server_domain: server_domain,
        server_port: server_port
    };

});
